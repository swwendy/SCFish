using UnityEngine;using System.Collections;using System.Collections.Generic;/// <summary>/// 公会类，成员加入，退出等功能/// </summary>public class Guild{    //公会数据    private GuildData m_GuildData;    public Guild()    {    }    //成员加入公会    public bool PlayerJoinGuild(uint playerId)    {        return true;    }    //成员退出公会    public bool PlayerQuitGuild(uint playerId)    {        return true;    }}